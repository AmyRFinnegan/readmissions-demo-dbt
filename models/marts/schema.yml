version: 2

models:
  - name: fact_readmissions
    columns:
      - name: patient_id
        tests:
          - not_null
      - name: encounter_id
        tests:
          - not_null
      - name: is_30day_readmit
        tests:
          - accepted_values:
              values: [0, 1]

